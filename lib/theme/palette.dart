import 'package:flutter/material.dart';

class Palette {
  // Colores originales
  static const sand  = Color(0xFFF3E5D0);
  static const brown = Color(0xFF7A5D48);
  static const mint  = Color(0xFF5FC8B3);

  // ➕ Colores añadidos para que compile el nuevo código
  static const text   = Color(0xFF1E1E1E); // texto principal
  static const accent = Color(0xFFC6813F); // color de acento (botones, detalles)
  static const teal   = Color(0xFF0E6B5B); // tono verde/azulado para enlaces
}
